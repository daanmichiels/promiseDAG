REM For Windows user who have node.js' http-server installed.
REM The option '-a localhost' binds to localhost, ensuring the
REM server only serves the computer it's running on (rather than
REM the entire local network).
http-server -a localhost
